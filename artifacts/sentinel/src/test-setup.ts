/**
 * Vitest global test setup for the Sentinel React app.
 *
 * - Extends vitest's `expect` with @testing-library/jest-dom matchers
 *   (toBeInTheDocument, toHaveClass, toBeVisible, etc.)
 */
import "@testing-library/jest-dom";
