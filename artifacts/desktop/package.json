{
  "name": "@workspace/desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:server": "node scripts/build-server.mjs",
    "make-icons": "node scripts/make-icons.mjs",
    "typecheck": "echo 'Desktop package has no TypeScript sources to check (Rust + shell scripts only).'"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0",
    "@tauri-apps/plugin-shell": "^2.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0"
  }
}
