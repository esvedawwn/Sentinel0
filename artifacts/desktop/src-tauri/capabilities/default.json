{
  "identifier": "default",
  "description": "Default Sentinel capabilities",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-open",
    "dialog:allow-open"
  ]
}
